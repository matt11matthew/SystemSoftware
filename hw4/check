#!/usr/bin/bash

echo "calling make clean"
sudo make clean

echo "calling make compiler"
sudo make compiler

clear
echo "========================================"
echo "Output"
echo "========================================"
sudo make check
